name: Trigger voidbr-pkgmake

on:
  push:
    branches: ["main"]

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Disparar build remoto
        run: |
          echo "Repo atual: ${{ github.event.repository.name }}"

          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.ORGANIZATION_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/voidbr/voidbr-pkgmake/dispatches \
            -d "{
              \"event_type\": \"webhook\",
              \"client_payload\": {
                \"package\": \"${{ github.event.repository.name }}\"
              }
            }"
